/**
 * @file linux_incs.h
 *
 * @date	15.03.2016
 * @author	michi
 */

#ifndef MAC_LINUX_INCS_H_
#define MAC_LINUX_INCS_H_


#include <stdbool.h>
#include <inttypes.h>

#define MAX(a,b) ((a) > (b) ? a : b)
#define MIN(a,b) ((a) < (b) ? a : b)


#endif /* MAC_LINUX_INCS_H_ */
